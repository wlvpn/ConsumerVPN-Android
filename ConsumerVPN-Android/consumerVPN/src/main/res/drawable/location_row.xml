<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/location_row_pressed_color" />
        </shape>
    </item>

    <!-- Focused -->
    <item android:state_focused="true">
        <shape>
            <solid android:color="@color/location_row_focused_color" />
        </shape>
    </item>

    <!-- Hovered -->
    <item android:state_hovered="true">
        <shape>
            <solid android:color="@color/location_row_pressed_color" />
        </shape>
    </item>

    <!-- Normal -->
    <item>
        <shape>
            <solid android:color="@color/location_row_color" />
        </shape>
    </item>

</selector>